| Hey #{@user.first_name},
| 
| Your project titled "#{@project.title}" has some new contributions:
| 
- if !@tasks.empty?
  | 
  | =====
  | New Tasks
  | ===
  - @tasks.each do |task|
    | - #{task.title}
  | =====
- if !@discussions.empty?
  | 
  | =====
  | New Discussions
  | ===
  - @discussions.each do |discussion|
    | - #{discussion.title}
  | =====
|
| Regards,
|
| Rails PM Tool